<h1>Edit Comment</h1>

<%= form_for([@post, @comment]) do |form| %>
  <%= render partial: '/comments/form', object: form %>

     <div class="actions">
        <%= form.submit %>
     </div>

<% end %>

<%= link_to 'Back', post_path(@post) %>