<h1>New Comment</h1>


<%= form_for(:comment, url: "/posts/#{@post.id}/comments", method: 'post') do |form| %>
  <%= render partial: '/comments/form', object: form %>

     <div class="actions">
        <%= form.submit %>
     </div>

<% end %>

<%= link_to 'Back', post_path(@post) %>