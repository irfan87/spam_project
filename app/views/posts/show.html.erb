<p id="notice"><%= notice %></p>

<p>
  <h5><%= @post.title %></h5>
</p>

<p>
    <div>
      <%= @post.body.html_safe %>
    </div>
</p>


<%= link_to 'New Comment', new_post_comment_path(@post) %><br>

  <tbody>
    <% @post.comments.each do |comment| %>
      <tr>
        <td><%= comment.body %></td>
        <!-- need to review & update who commented this -->
        </tr><br>
    <% end %>
  </tbody>

<br>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
